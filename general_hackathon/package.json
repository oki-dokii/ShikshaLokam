{
  "name": "workspace",
  "version": "1.0.0",
  "description": "A multi-page FastAPI application that uses Google Gemini 2.5 Flash to extract structured JSON from DPR (Detailed Project Report) PDFs and provides an interactive chat interface to ask questions about the documents.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "recharts": "^3.4.1"
  }
}
